
import aiohttp
import requests

# connector = aiohttp.TCPConnector(verify_ssl=False)
# http_session = aiohttp.ClientSession(connector=connector, timeout=session_timeout, trust_env=True)

# print(.get("https://8tz.ru/"))

print(requests.get("https://8tz.ru/"))